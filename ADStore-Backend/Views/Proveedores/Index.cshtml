@model ADStoreBackend.Models.PaginatedViewModel<ADStoreBackend.Models.ProveedorDto>
@{
    ViewData["Title"] = "Proveedores";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Proveedores</h2>
    <a asp-action="Create" class="btn btn-primary">+ Nuevo proveedor</a>
</div>

<table class="table table-bordered table-hover">
    <thead class="table-dark">
        <tr>
            <th>#</th>
            <th>Nombre</th>
            <th>Teléfono</th>
            <th>Dirección</th>
            <th>Estado</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var p in Model.Items)
        {
            <tr>
                <td>@p.Id</td>
                <td>@p.Nombre</td>
                <td>@(p.Telefono ?? "—")</td>
                <td>@(p.Direccion ?? "—")</td>
                <td>
                    <span class="badge @(p.Activo ? "bg-success" : "bg-secondary")">
                        @(p.Activo ? "Activo" : "Inactivo")
                    </span>
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@p.Id" class="btn btn-sm btn-warning">Editar</a>
                    <form asp-action="Delete" asp-route-id="@p.Id" method="post" class="d-inline"
                          onsubmit="return confirm('¿Eliminar @p.Nombre?')">
                        <button type="submit" class="btn btn-sm btn-danger">Eliminar</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<partial name="_Paginacion" model="Model" />